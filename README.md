# Delete From Editor Tab README

Simple extension that adds a "Delete" option to the context menu for editor tabs in VSCode.

It will move the file to the trash using.  (Internally it uses `moveFileToTrash`.)
